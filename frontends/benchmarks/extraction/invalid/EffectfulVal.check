[ Error  ] EffectfulVal.scala:8:9: A field defined in an object must not depend on mutable state, but rand is not referentially transparent
               val rand = Random.nextBigInt(state)
                   ^
