[ Error  ] EffectfulVal.scala:8:9: A field defined in an object must not reference mutable objects, but it references thiss of mutable type Test
               val rand = Random.nextBigInt(state)
                   ^
